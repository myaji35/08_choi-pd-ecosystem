# Vercel 환경 변수 설정 가이드
# 이 파일을 참고하여 Vercel 대시보드에서 환경 변수를 설정하세요

# ====================================
# 필수 환경 변수 (Required)
# ====================================

# 앱 URL (Vercel에서 자동 생성되는 URL 또는 커스텀 도메인)
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app

# 데이터베이스 (SQLite는 서버리스 환경에서 제한적)
# Vercel에서는 Postgres, MySQL 등 외부 DB 사용 권장
DATABASE_URL=file:./data/database.db

# ====================================
# Clerk 인증 (Authentication)
# ====================================

# Clerk 퍼블릭 키 (프론트엔드용)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxx

# Clerk 시크릿 키 (백엔드용)
CLERK_SECRET_KEY=sk_live_xxxxxxxxxxxxx

# Clerk 라우팅 설정
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/admin/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/admin/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/admin/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/admin/dashboard

# ====================================
# 보안 설정 (Security)
# ====================================

# 암호화 키 (32자 이상의 랜덤 문자열)
ENCRYPTION_KEY=your-super-secret-encryption-key-min-32-chars

# ====================================
# 개발/프로덕션 모드
# ====================================

# 개발 모드 (프로덕션에서는 반드시 false)
NEXT_PUBLIC_DEV_MODE=false

# Node 환경
NODE_ENV=production

# ====================================
# 선택적 환경 변수 (Optional)
# ====================================

# Google Cloud Storage (이미지 업로드용)
# GCS_PROJECT_ID=your-project-id
# GCS_BUCKET_NAME=your-bucket-name
# GCS_CREDENTIALS={"type":"service_account",...}

# 이메일 서비스 (SendGrid, AWS SES 등)
# EMAIL_SERVICE=sendgrid
# EMAIL_API_KEY=your-api-key
# EMAIL_FROM=noreply@impd.co.kr

# 분석 도구
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Sentry 에러 모니터링
# SENTRY_DSN=https://xxxxx@xxx.ingest.sentry.io/xxxxx
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxx.ingest.sentry.io/xxxxx

# ====================================
# Vercel 전용 설정
# ====================================

# Vercel Analytics (자동 설정됨)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=auto

# Edge Config (선택사항)
# EDGE_CONFIG=https://edge-config.vercel.com/xxxxx

# ====================================
# 환경별 설정 방법
# ====================================

# 1. Production 환경
#    - Vercel Dashboard → Settings → Environment Variables
#    - Production 체크박스 선택
#    - 위의 값들 입력

# 2. Preview 환경 (PR 프리뷰)
#    - Preview 체크박스 선택
#    - 테스트용 값 입력 가능

# 3. Development 환경 (로컬)
#    - Development 체크박스 선택
#    - 또는 .env.local 파일 사용

# ====================================
# 주의사항
# ====================================

# 1. NEXT_PUBLIC_ 접두사가 있는 변수는 클라이언트에 노출됨
# 2. 민감한 정보는 절대 NEXT_PUBLIC_ 접두사 사용 금지
# 3. Vercel에서 Encrypted 옵션 사용 가능 (민감한 값)
# 4. 변경 후 재배포 필요